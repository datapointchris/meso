# Keep the web build context small: node_modules is rebuilt via `npm ci`, and
# the Go trees / planning / VCS metadata are irrelevant to the SPA build.
node_modules
dist
api
cli
.git
.github
.planning
*.md
!index.html
.DS_Store
